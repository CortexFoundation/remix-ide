import { ModuleProfile, Api, ApiEventEmitter, PluginRequest, PluginApi } from '../types';
export declare abstract class ApiFactory<T extends Api = any> {
    abstract readonly profile: ModuleProfile<T>;
    private requestQueue;
    protected currentRequest: PluginRequest;
    events?: ApiEventEmitter<T>;
    activate?: () => Promise<void>;
    deactivate?: () => void;
    render?: () => HTMLElement;
    api(): PluginApi<T>;
}
